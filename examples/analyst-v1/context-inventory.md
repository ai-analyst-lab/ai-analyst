# Context inventory

The supplied example references the course `.knowledge` package. Before company use, replace it with approved company definitions, owners, review dates, and evidence. A context trace proves supply, not correct use.
