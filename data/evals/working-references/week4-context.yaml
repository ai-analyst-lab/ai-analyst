suite_id: week4-context
suite_version: '1'
data_snapshot: novamart-course-context-2026-09-06
purpose: Reviewed working cases for the Session 8 context-change lab
cases:
  - case_id: membership-retention-current
    task: Build the deterministic context trace for "What is our membership retention rate?" Report selection_status as current_only, current_and_legacy, legacy_only, or neither based on which membership metric definitions the trace supplied.
    exposure: working
    purpose: capability
    case_version: '1'
    status: verified
    truth_basis: expert
    data_scope:
      context_policy_test:
        type: selection
        dataset: novamart
        question: What is our membership retention rate?
        current_item: metric:membership_retention
        legacy_item: metric:legacy_active_member_rate
        today: '2026-09-06'
    success_criteria:
      - type: exact
        description: A general retention question receives the reviewed current definition without the legacy dashboard definition.
    allowed_tools: [read_data]
    required_behavior: [Use the deterministic context manifest rather than guessing from filenames.]
    forbidden_behavior: []
    expected: current_only
    tolerance: {}
    graders:
      - id: current-context-selection
        type: exact
        field: selection_status
        allowed_values: [current_only, current_and_legacy, legacy_only, neither]
    slices: {role: target, mechanism: delivery}
    data_snapshot: novamart-course-context-2026-09-06
    truth_evidence: Reviewed delivery policy for general current-retention questions.
    reproduction: Build the context manifest and inspect the two membership metric item IDs.
    author: AI Analyst Lab
    reviewed_by: Shane Butler
    verified_at: '2026-09-06T00:00:00+00:00'
  - case_id: completed-revenue
    task: Route the NovaMart total_revenue metric and report route as compiled, external, contract-guided, generated, or generated-unverified.
    exposure: working
    purpose: regression
    case_version: '1'
    status: verified
    truth_basis: computed
    data_scope:
      context_policy_test:
        type: metric-route
        dataset: novamart
        metric: total_revenue
    success_criteria:
      - type: exact
        description: The existing total-revenue execution route does not change.
    allowed_tools: [read_data]
    required_behavior: [Use the metric router rather than inferring from the metric name.]
    forbidden_behavior: []
    expected: compiled
    tolerance: {}
    graders:
      - id: revenue-route
        type: exact
        field: route
        allowed_values: [compiled, external, contract-guided, generated, generated-unverified]
    slices: {role: regression, mechanism: execution}
    data_snapshot: novamart-course-context-2026-09-06
    truth_evidence: Deterministic metric-router result for the reviewed total-revenue contract.
    reproduction: Route total_revenue with helpers.data.metric_router.
    author: AI Analyst Lab
    reviewed_by: Shane Butler
    verified_at: '2026-09-06T00:00:00+00:00'
  - case_id: historical-active-members
    task: Build the deterministic context trace for "What did the archived dashboard report as historical membership retention at month-end?" Report selection_status as current_only, current_and_legacy, legacy_only, or neither based on which membership metric definitions the trace supplied.
    exposure: working
    purpose: regression
    case_version: '1'
    status: verified
    truth_basis: expert
    data_scope:
      context_policy_test:
        type: selection
        dataset: novamart
        question: What did the archived dashboard report as historical membership retention at month-end?
        current_item: metric:membership_retention
        legacy_item: metric:legacy_active_member_rate
        today: '2026-09-06'
    success_criteria:
      - type: exact
        description: Explicit historical intent can still receive the legacy definition alongside the current definition.
    allowed_tools: [read_data]
    required_behavior: [Use the deterministic context manifest rather than guessing from filenames.]
    forbidden_behavior: []
    expected: current_and_legacy
    tolerance: {}
    graders:
      - id: historical-context-selection
        type: exact
        field: selection_status
        allowed_values: [current_only, current_and_legacy, legacy_only, neither]
    slices: {role: regression, mechanism: delivery}
    data_snapshot: novamart-course-context-2026-09-06
    truth_evidence: Reviewed compatibility requirement for the archived dashboard definition.
    reproduction: Build the context manifest and inspect the two membership metric item IDs.
    author: AI Analyst Lab
    reviewed_by: Shane Butler
    verified_at: '2026-09-06T00:00:00+00:00'
